name: Build HomeCore

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    name: Build firmware
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        build_type: [Debug, Release]
        board: [qemu_lm3s6965evb]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            ninja-build \
            gcc-arm-none-eabi \
            binutils-arm-none-eabi \
            python3 \
            python3-venv \
            python3-pip

      - name: Setup Python venv
        run: |
          python3 -m venv .venv
          . .venv/bin/activate
          python -m pip install --upgrade pip
          pip install kconfiglib
      
      - name: Configure
        run: |
          . .venv/bin/activate
          cmake -S . -B build-${{ matrix.build_type }}-${{ matrix.board }} \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
            -DHOMECORE_BOARD=${{ matrix.board }} \
            -DCMAKE_TOOLCHAIN_FILE=cmake/toolchains/arm-none-eabi.cmake \

      - name: Build
        run: |
          cmake --build build-${{ matrix.build_type }}-${{ matrix.board }} --verbose

      - name: Firmware size
        run: |
          find build-${{ matrix.build_type }} -type f \( -name "*.elf" -o -name "homecore" \) -exec arm-none-eabi-size {} \;

      - name: Upload firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: homecore-${{ matrix.build_type }}
          path: |
            build-${{ matrix.build_type }}/*.elf
            build-${{ matrix.build_type }}/*.bin
            build-${{ matrix.build_type }}/*.hex
            build-${{ matrix.build_type }}/*.map